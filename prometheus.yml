global:
  scrape_interval: 5s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'shortin-api'
    metrics_path: '/actuator/prometheus'
    static_configs:
      - targets: ['shortin-api:8080']
        labels:
          application: 'shortin-api'