services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=shortin_db'
      - 'POSTGRES_PASSWORD=${DB_PASSWORD}'
      - 'POSTGRES_USER={DB_USER}'
    ports:
      - '5432'